<h1>Welcome to the <%=@network.name%> Page!</h1>
<table class="table">
<tbody>
  <%@network.shows.order(title: :asc).each do |show|%>
    <tr ><td style="border:none"><%=link_to show.title, show_path(show)%></td></tr>
    <%end%>
</tbody>
</table>
