<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_url %>">TV Tracker</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Shows <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="<%=shows_path%>">View All Shows <span class="sr-only">(current)</span></a></li>
            <%if user_signed_in?%>
            <li role="separator" class="divider"></li>
            <li><%=link_to "Add a New Show!", new_user_show_path(current_user) if user_signed_in?%></li>
            <%end%>
          </ul>
        </li>
        <li><a href="<%=networks_path%>">Networks</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%=user_signed_in? ? "Profile" : "Sign In"%> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <%if !user_signed_in?%>
            <li><%=link_to "Sign Up", new_user_registration_path %></li>
            <li><%=link_to "Sign In", new_user_session_path%></li>
            <%else%>
            <li><%=link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
            <li><%=link_to "View Your Show Collection", user_path(current_user)%></li>
            <li><%=link_to "Edit Your Details", edit_user_registration_path%></li>
            <%end%>
            <li><%=link_to "View Users", users_path%></li>
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
      <%= form_tag search_path, method: :post, :class => "navbar-form navbar-left", :id=>"showSearch" do %>
        <%= text_field_tag :query, params[:query], :class => "form-control", :id => "showName", :placeholder => "Search for a show" %>
        <%= submit_tag "Search", name: nil, class: "btn btn-default showSearch" %>   
      <% end %>
        
      </ul>
    </div>
  </div>
</nav>




